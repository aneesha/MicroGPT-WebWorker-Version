MicroGPT-Webworker-Version

Pure index.html file that uses Pyodide.

Can install any js libs and use node command line. 

The project uses git. 

Always write tests and run them after changes.

Code goes in src folder.

Final output can be placed dist folder. 
