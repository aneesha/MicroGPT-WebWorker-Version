{
  "name": "microgpt-webworker-version",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "mkdir -p dist/data && cp src/index.html dist/index.html && cp src/main.js dist/main.js && cp src/worker.js dist/worker.js && cp src/utils.js dist/utils.js && cp src/data/addition_examples.csv dist/data/addition_examples.csv"
  }
}
